info:
  name: パスワード更新（401エラー）
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3000/api/my-data/password
  body:
    type: json
    data: |-
      {
        "password": "wrongpassword",
        "newPassword": "newpassword"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
