info:
  name: ユーザー情報取得（404エラー）
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3000/api/admin/users/404
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
