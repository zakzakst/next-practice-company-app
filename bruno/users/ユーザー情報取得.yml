info:
  name: ユーザー情報取得
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3000/api/admin/users/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
