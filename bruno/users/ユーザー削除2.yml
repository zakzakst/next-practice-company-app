info:
  name: ユーザー削除2
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:3000/api/admin/users
  body:
    type: json
    data: |-
      {
        "id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
