info:
  name: ユーザー情報一覧取得（絞り込み）
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/api/admin/users?q=Taro
  params:
    - name: q
      value: Taro
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
