info:
  name: ユーザー更新
  type: http
  seq: 6

http:
  method: PUT
  url: http://localhost:3000/api/admin/users/2
  body:
    type: json
    data: |-
      {
        "name": "更新 氏名",
        "email": "new@example.com",
        "roles": ["user"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
