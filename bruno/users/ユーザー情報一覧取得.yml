info:
  name: ユーザー情報一覧取得
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/admin/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
